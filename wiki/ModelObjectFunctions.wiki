#summary Listing of all model object methods.
#labels chapter,1.0

These methods operate on individual objects which means you first have to fetch or create an object and then call the method on that object. You can call these methods from anywhere but it is not recommended to call them from view pages. When calling them from its own model file it is recommended to reference the scope explicitly with the `this` keyword.

==Example==

`controllers/Blog.cfc`:

{{{
<cfset anAuthor = model("author").findByKey(1)>
<cfset anAuthor.delete()>
}}}

==Functions==

*Associations:*

 * [updateByKey addXXX()] (`hasMany`)
 * [create createXXX()] (`hasMany`)
 * [deleteByKey deleteXXX()] (`hasMany`)
 * [deleteAll deleteAllXXX()] (`hasMany`)
 * [findOne findOneXXX()] (`hasMany`)
 * [exists hasXXX()] (`hasMany`)
 * [new newXXX()] (`hasMany`)
 * [updateByKey removeXXX()] (`hasMany`)
 * [updateAll removeAllXXX()] (`hasMany`)
 * [findAll XXX()] (`hasMany`)
 * [count XXXCount()] (`hasMany`)

 * [create createXXX()] (`hasOne`)
 * [deleteOne deleteXXX()] (`hasOne`)
 * [exists hasXXX()] (`hasOne`)
 * [new newXXX()] (`hasOne`)
 * [updateOne removeXXX()] (`hasOne`)
 * [updateByKey setXXX()] (`hasOne`)
 * [findOne XXX()] (`hasOne`)

 * [exists hasXXX()] (`belongsTo `)
 * [findByKey XXX()] (`belongsTo`)

*Changes:*
 * [allChanges allChanges()]
 * [changedFrom changedFrom()]
 * [changedProperties changedProperties()]
 * [hasChanged hasChanged()]
 * [hasChanged XXXHasChanged()]
 * [changedFrom XXXChangedFrom()]

*CRUD:*
 * [delete delete()]
 * [save save()]
 * [update update()]

*Errors:*
 * [addError addError()]
 * [addErrorToBase addErrorToBase()]
 * [allErrors allErrors()]
 * [clearErrors clearErrors()]
 * [errorCount errorCount()]
 * [errorsOn errorsOn()]
 * [errorsOnBase errorsOnBase()]
 * [hasErrors hasErrors()]
 * [valid valid()]

*Miscellaneous:*
 * [isNew isNew()]
 * [key key()]
 * [reload reload()]